- never run code without first asking the user for permission.
- when running code, first use `poetry shell`
- when running anything that needs secrets, use `doppler run --project whatsapp_miner_backend --config dev_personal`. Also add that to shell scripts
